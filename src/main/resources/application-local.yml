server:
  port: ${CATCONNECT_TOMCAT_PORT:8080}
spring:
  datasource:
    url: ${CATCONNECT_LOCAL_DB_URL}/${CATCONNECT_LOCAL_DB_NAME}
    username: ${CATCONNECT_LOCAL_DB_USERNAME}
    password: ${CATCONNECT_LOCAL_DB_PASSWORD}
    driver-class-name: org.postgresql.Driver